import { LengthConverterPipe } from './length-converter.pipe';

describe('LengthConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
